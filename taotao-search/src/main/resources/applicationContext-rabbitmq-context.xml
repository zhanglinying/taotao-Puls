<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/rabbit
	http://www.springframework.org/schema/rabbit/spring-rabbit-1.4.xsd
	http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-4.1.xsd">

    <!-- 定义RabbitMQ的连接工厂 -->
    <rabbit:connection-factory id="connectionFactory"
                               host="193.112.217.168" port="5672" username="root" password="123456"
                               virtual-host="/root"/>
    <!-- 定义管理 -->
    <rabbit:admin connection-factory="connectionFactory"/>
    <!-- 定义队列 -->
    <rabbit:queue name="taotao-search-item-queue" auto-declare="true" durable="true"/>
    <!-- 定义消费者 -->
    <bean id="itemMQHandler" class="com.taotao.search.mq.handler.ItemMQHandler"></bean>

    <!-- 监听 -->
    <rabbit:listener-container connection-factory="connectionFactory">
            <rabbit:listener ref="itemMQHandler" method="Foo" queue-names="taotao-search-item-queue"></rabbit:listener>
    </rabbit:listener-container>
</beans>